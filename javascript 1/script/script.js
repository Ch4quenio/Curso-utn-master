//  || Indica que para que el if se ejecute se tienen que cumplir cualquiera de las dos condiciones. Ejemplo:

let dato = 20;

if (dato > 30 || dato == 20){ //Si dato es mayor que 30 el codigo se ejecuta, Si dato no es mayor que 30 pero es igual a 20 se ejecuta igualmente
  document.write("sos crack");
}


// && ambas condiciones se tienen que cumplir para que el codigo se ejecute, si una se cumple pero la otra no, entonces el código no se va a ejecutar.
let nombre = "El gordo tony"

if (dato == 20 && nombre == "El gordo tony"){
  document.write(" <br>Bien amigo, vamos aprendiendo JS")
}
